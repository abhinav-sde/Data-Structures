
public class core_problems {
    /**
     * Discussion: Sparse Table vs Segment Tree.
     * Sparse Table: O(N log N) precompute, O(1) Query. Static only (no updates).
     * Segment Tree: O(N) build, O(log N) Query, O(log N) Update.
     * Problem Pattern: Use Sparse Table when the array is static and you need
     * constant time queries.
     */
}
