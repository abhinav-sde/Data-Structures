
public class advanced_problems {
    /**
     * Problem: Range GCD Query using Sparse Table.
     * Since GCD is idempotent (gcd(a, a) = a), Sparse Table works perfectly for
     * O(1) GCD queries.
     */
}
